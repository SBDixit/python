# TUPLES is a types of data structure ,it is basically a container where we store different values in single variables.

coordinates = (4,5)
coordinates [0] = 10    # actually in tuples modifiction is not allowed means u can't change the coordinates
print(coordinates)